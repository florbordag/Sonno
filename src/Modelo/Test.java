/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package Modelo;

import Data.BandaData;
import Data.Conexion;

/**
 *
 * @author Flor
 */
public class Test {

    /**
     * @param args the command line arguments
     */
  /*  public static void main(String[] args) {
        // TODO code application logic here
        Conexion conexion;
  try {             //<<< CREAR HUESPED >>>
            conexion = new Conexion("jdbc:mysql://localhost/sonno", "root", "");
            BandaData bd = new BandaData(conexion);
     
              Banda band = new Banda("Las Babosas", "0");
             bd.registrarBanda(band);
                    //<< DEVOLVER DATOS SI SE REQUIERE >>
              System.out.println("El huesped a sido creado con éxito =)");
                    } catch (Exception e) {
            System.out.println("Error al instanciar la clase conexion crear huesped: " + e.getMessage());}}  */
                    }
