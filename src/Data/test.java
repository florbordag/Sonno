/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package Data;

import Modelo.Admin;
import Modelo.Banda;
import Modelo.Turno;
import java.time.LocalDateTime;
import java.util.logging.Level;
import java.util.logging.Logger;

/**
 *
 * @author Flor
 */
public class test {
private static Conexion con;

    /**
     * @param args the command line arguments
     */
    public static void main(String[] args) {
/*    try {
        Admin admin = new Admin(1, "flor", "123");
                Banda banda = new Banda(5, "La nueva Luna", 0, "Todos los domingos todos los domingos");
      //  Turno turno = new Turno(1, admin, banda, fecha , 300, 1);

        
        con = new Conexion("jdbc:mysql://localhost/sonno", "root", "");
        AdminData ad = new AdminData(con); BandaData bd = new BandaData(con); TurnoData td = new TurnoData(con);
    } catch (ClassNotFoundException ex) {
        Logger.getLogger(test.class.getName()).log(Level.SEVERE, null, ex);
    }
 
 
 
 
 
    }  */
}}

